#! /bin/sh

ORIGIN=origin                                    # git repo to push
REMOTE="https://github.com/mulle-objc"
ARCHIVEURL='${REMOTE}/${NAME}/archive/${VERSION}.tar.gz'  # ARCHIVEURL will be evaled later! keep it in single quotes
